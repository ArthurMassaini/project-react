import React from 'react';

function OrderForm() {
  return (
    <form className="form">
      <select>
        <option>ordenar por: </option>
        <option>ordenar por: dois</option>
        <option>ordenar por: tres</option>
      </select>
    </form>
  );
}

export default OrderForm;
